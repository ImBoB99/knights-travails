function knightMoves(startingVertex, finalVertex) {
  // TO DO
  // [x, y]
  // 8x8 grid
  // knight movement can be: 2 steps forward 1 side step || 1 step fowards 2 side steps
  let knightPosition = startingVertex;
  let knightDirections = [[1,2], [2, 1]]
}

knightMoves([0, 0], [1, 2]);
